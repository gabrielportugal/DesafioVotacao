version: "3.9"
services:
  app:
    volumes:
      - ./src:/app/src
      - ./wait-for-it.sh:/wait-for-it.sh
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DEVTOOLS_RESTART_ENABLED: "true"
